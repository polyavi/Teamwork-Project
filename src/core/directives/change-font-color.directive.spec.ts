/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ChangeFontColorDirective } from './change-font-color.directive';

describe('ChangeFontColorDirective', () => {
  it('should create an instance', () => {
    let directive = new ChangeFontColorDirective();
    expect(directive).toBeTruthy();
  });
});
