/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ChangeImageWithDefaultDirective } from './change-image-with-default.directive';

describe('ChangeImageWithDefaultDirective', () => {
  it('should create an instance', () => {
    let directive = new ChangeImageWithDefaultDirective();
    expect(directive).toBeTruthy();
  });
});
